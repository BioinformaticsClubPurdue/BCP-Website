import { extendTheme } from '@chakra-ui/react';

export default extendTheme({
  colors: {
    scheme: {
      main: 'rebeccapurple',
    },
  },
  fonts: {
    heading: 'Helvetica',
    body: 'Helvetica',
  },
});
